
import React from 'react';

interface MediaPanelProps {
  // Add props as needed
}

const MediaPanel: React.FC<MediaPanelProps> = () => {
  return (
    <div>
      {/* Content will be handled by MediaEditor component */}
    </div>
  );
};

export default MediaPanel;
